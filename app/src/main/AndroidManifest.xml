<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rahmawati.eli.toco" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Shop"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".kategories.kategories"
            android:label="@string/kategories"
            android:parentActivityName=".Shop" >
        </activity>
        <activity
            android:name=".kategories.kategoriesdetail"
            android:label="@string/title_activity_kategoriesdetail" >
        </activity>
    </application>

</manifest>
